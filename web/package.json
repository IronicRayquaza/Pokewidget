{
  "name": "pokewidget-web",
  "version": "1.5.0",
  "private": true,
  "type": "module",
  "description": "PokeWidget for desktops and browsers: the same widgets, outside Android.",
  "scripts": {
    "sync-data": "node scripts/sync-data.mjs",
    "dev": "npm run sync-data && vite",
    "build": "npm run sync-data && tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "npm run sync-data && vitest run",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.1",
    "@tauri-apps/cli": "^2.11.4",
    "@types/node": "^22.20.3",
    "puppeteer-core": "^25.11.0",
    "typescript": "^5.7.3",
    "vite": "^6.0.11",
    "vitest": "^3.0.5"
  },
  "dependencies": {
    "preact": "^10.25.4"
  }
}
